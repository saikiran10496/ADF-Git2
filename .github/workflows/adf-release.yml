name: ADF Release Pipeline

on:
  release:
    types: [published]

permissions:
  id-token: write
  contents: read

jobs:
  release:
    environment: production
    runs-on: ubuntu-latest     
    steps:        
      - name: Download Release Asset
        uses: dsaltares/fetch-gh-release-asset@master
        with:
          repo: ${{ github.repository }}
          version: latest
          file: adfexport.zip
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Unzip Artifact
        run: unzip adfexport.zip -d ${{ github.workspace }}

      - name: Debug - Check Downloaded Files
        run: ls -R ${{ github.workspace }}/export || echo "No files found"

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          enable-AzPSSession: true
          
      - name: Deploy to Dev Data Factory
        uses: Azure/data-factory-deploy-action@v1.2.0
        with:
          resourceGroupName: ${{ secrets.RESOURCE_GROUP }}
          dataFactoryName: ${{ secrets.DATA_FACTORY_NAME }}
          armTemplateFile: ${{ github.workspace }}/build/export/ARMTemplateForFactory.json
          armTemplateParametersFile: ${{ github.workspace }}/build/export/ARMTemplateParametersForFactory.json
